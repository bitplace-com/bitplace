

# Change "Used PE Allocation" label to "PE Allocation"

Update the label text in `src/components/modals/PixelControlPanel.tsx` (line 199):
- Change `Used PE Allocation` to `PE Allocation`
- Also update the comment on line 195 from `{/* Used PE Allocation */}` to `{/* PE Allocation */}`

Single file, two-line change.

